import {SectionId} from "@/lib/constants";
import React from "react";

const ProjectsSection = () => {
	return (
		<section id={SectionId.PROJECTS} style={{height: "100vh"}}>
			Projects section
		</section>
	);
};

export default ProjectsSection;
